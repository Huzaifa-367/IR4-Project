; Project PHP settings required for large CSV/XLSX imports.
; App-level worker import still validates max 15MB (ImportWorkersRequest).
upload_max_filesize = 64M
post_max_size = 64M
memory_limit = 512M
max_execution_time = 300
